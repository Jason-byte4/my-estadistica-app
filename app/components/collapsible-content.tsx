// app/components/ui/collapsible-content.tsx
import React from 'react';

const CollapsibleContent = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export default CollapsibleContent;
